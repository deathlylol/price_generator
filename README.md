# Генератор ценников

PHP скрипт для автоматической генерации HTML ценников из Excel файлов.

## Структура проекта

```
prices/
├── excel/                    # Excel файлы с данными товаров
│   ├── accessories.xlsx     # Аксессуары
│   ├── promotions.xlsx      # Акционные товары
│   └── simple.xlsx          # Обычные товары (смартфоны)
├── results/                  # Сгенерированные HTML ценники
│   ├── accessories/         # Только HTML файлы аксессуаров
│   ├── promotions/          # Только HTML файлы акций
│   └── simple/              # Только HTML файлы обычных товаров
├── assets/                  # 🆕 Общие ресурсы
│   └── images/             # SVG иконки и изображения
├── templates/               # HTML шаблоны с CSS
│   ├── accessories/        # Шаблон для аксессуаров
│   ├── promotions/         # Шаблон для акций
│   └── simple/             # Шаблон для обычных товаров
├── docker/                  # Docker конфигурация
│   ├── Dockerfile          # Docker образ
│   ├── docker-compose.yml  # Docker Compose конфигурация
│   ├── .dockerignore       # Исключения для Docker
│   └── README.md           # Документация Docker
├── scripts/                 # Исполняемые скрипты
│   ├── run.sh              # Скрипт запуска через Docker
│   ├── dev.sh              # Скрипт для режима разработки
│   ├── install.sh          # Скрипт локальной установки
│   └── README.md           # Документация скриптов
├── price_generator.php      # Основной скрипт генератора
├── composer.json           # Зависимости PHP
└── CHANGELOG.md            # История изменений
```

## Установка

### Вариант 1: Docker (рекомендуется)

1. Убедитесь, что у вас установлены Docker и docker-compose
2. Запустите генератор:
```bash
chmod +x scripts/run.sh
./scripts/run.sh
```

### Вариант 2: Локальная установка

1. Установите зависимости:
```bash
chmod +x scripts/install.sh
./scripts/install.sh
```

Или вручную:
```bash
composer install
```

2. Запустите генератор:
```bash
php price_generator.php
```

## Использование

### Docker
```bash
# Обычный запуск
./scripts/run.sh

# Режим разработки
chmod +x scripts/dev.sh
./scripts/dev.sh
```

### Локально
```bash
php price_generator.php
```

### Просмотр результатов
Для корректного отображения изображений запустите локальный веб-сервер:
```bash
./scripts/serve.sh
```
Затем откройте браузер: http://localhost:8000

## Формат Excel файлов

### Аксессуары (accessories.xlsx)
Ожидаемые колонки:
- Название
- Описание  
- Цена
- Артикул

### Акционные товары (promotions.xlsx)
Ожидаемые колонки:
- Название
- Описание
- Старая цена
- Цена
- Артикул

### Обычные товары (simple.xlsx)
Ожидаемые колонки:
- Название
- Модель
- Характеристики
- Цена
- Артикул

## Результат

После запуска в папке `results/` будут созданы HTML файлы с ценниками, организованные по типам товаров. Каждый ценник будет содержать:

- Название товара
- Описание/характеристики
- Цену (для акций - старую и новую)
- Артикул
- Стилизованное оформление

## Настройка

Вы можете изменить стили ценников, отредактировав метод `getBaseHtmlTemplate()` в файле `price_generator.php`.
